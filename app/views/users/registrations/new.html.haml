%h2 Sign up

= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class: "form-horizontal"}) do |f|
  = render "devise/shared/error_messages", resource: resource

  .form-group
    %label.control-label.col-sm-2 Phone Number
    .col-sm-10
      = f.phone_field :phone_number, autofocus: true, autocomplete: "phone_number", class: "form-control"

  .form-group
    %label.control-label.col-sm-2 Password
    .col-sm-10
      - if @minimum_password_length
        %em (#{@minimum_password_length} characters minimum)
      = f.password_field :password, autocomplete: "new-password", class: "form-control"

  .form-group
    %label.control-label.col-sm-2 First Name
    .col-sm-10
      = f.text_field :first_name, class: "form-control"

  .form-group
    %label.control-label.col-sm-2 Last Name
    .col-sm-10
      = f.text_field :last_name, class: "form-control"

  .form-group
    .mt-3.mb-3
      = f.submit "Sign up", class: "btn btn-primary"

.mt-3.mb-3
  = render "devise/shared/links"
